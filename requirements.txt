# These requirements are already installed by default in many 
# current GNU/Linux distributions (04/2020). To check it:
# pip3 list | grep "PyGObject\|requests"

# PyGObject>=3.36.0 (to install: sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0)
requests
